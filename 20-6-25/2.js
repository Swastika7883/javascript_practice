//2. Given an array of numbers [1, 2, 3, 4, 5], use the map() method to create a new array with each number doubled.

let nums =[1,2,3,4,5]

let doubled = nums.map(num=>num*2);

console.log(doubled);
