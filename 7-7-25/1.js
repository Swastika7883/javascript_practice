//1)extracting all alphabets from the given number..

let alpha = "badnmbmncb@#$%%";
let digit;

function extractDigits(alpha){
    while(alpha){
        digit = num %10;
        num= Math.floor(num /10);
        console.log(digit);
    }
    

}

extractDigits(num);